# Bridge Eval Prompt Template

Use this prompt when evaluating a model against `questions-bridge-v1.md`:

Answer only from the repo knowledge and stated architecture constraints.
If something is uncertain, say so.
Do not invent bridge features, permissions, mutation tools, callback behavior, or transport guarantees.
Be explicit about read-only v1 boundaries and safety rules.
